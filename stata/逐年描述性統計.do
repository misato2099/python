summarize receipt sales store pwrate pwusr wtrate if strpos(string(time), "105") > 0
summarize receipt sales store pwrate pwusr wtrate if strpos(string(time), "106") > 0
summarize receipt sales store pwrate pwusr wtrate if strpos(string(time), "107") > 0
summarize receipt sales store pwrate pwusr wtrate if strpos(string(time), "108") > 0

codebook receipt sales store pwrate pwusr wtrate if strpos(string(time), "105") > 0
codebook receipt sales store pwrate pwusr wtrate if strpos(string(time), "106") > 0
codebook receipt sales store pwrate pwusr wtrate if strpos(string(time), "107") > 0
codebook receipt sales store pwrate pwusr wtrate if strpos(string(time), "108") > 0